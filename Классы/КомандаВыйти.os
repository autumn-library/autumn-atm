&Пластилин
Перем ТекущийПользователь;

Функция ВыполнитьКоманду(Знач Параметры) Экспорт
	Если Параметры.Количество() <> 1 Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Сообщить("Пока " + ТекущийПользователь.Получить().ИмяПользователя);

	ТекущийПользователь.Установить(Неопределено);

	Возврат Истина;
КонецФункции

&Желудь
&Прозвище("Команда")
Процедура ПриСозданииОбъекта()
КонецПроцедуры