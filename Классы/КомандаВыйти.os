&Пластилин
Перем ХранительБД Экспорт;

Функция ВыполнитьКоманду(Знач Параметры) Экспорт
	Если Параметры.Количество() <> 1 Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Сообщить("Пока " + ХранительБД.ТекущийПользователь.ИмяПользователя);

	ХранительБД.ТекущийПользователь = Неопределено;

	Возврат Истина;
КонецФункции

&Желудь
&Прозвище("Команда")
Процедура ПриСозданииОбъекта()
КонецПроцедуры